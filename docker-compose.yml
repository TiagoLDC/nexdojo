services:
  # Frontend (React + Vite) via Nginx
  nexdojo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nexdojo-qas
    restart: always
    ports:
      - "3003:80"
    networks:
      - nexdojo-net

  # API Backend (Node.js + Express + Prisma)
  nexdojo-api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: nexdojo-api
    restart: always
    ports:
      - "3005:3005"
    environment:
      - PORT=3005
      - DATABASE_URL=mysql://qasnexdojo_qas:yFkL8OvxPnr3@162.240.167.149:3306/qasnexdojo_qas
      - JWT_SECRET=nexdojo_super_secret_jwt_key_2026
    networks:
      - nexdojo-net

networks:
  nexdojo-net:
    driver: bridge
